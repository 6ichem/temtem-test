{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "run-api": "turbo run dev",
    "run-ios": "turbo run start-ios",
    "run-android": "turbo run start-android"
  },
  "devDependencies": {
    "@turbo/gen": "^1.9.7",
    "turbo": "latest"
  },
  "name": "temtem-test",
  "packageManager": "yarn@1.22.15",
  "workspaces": [
    "apps/*"
  ]
}
